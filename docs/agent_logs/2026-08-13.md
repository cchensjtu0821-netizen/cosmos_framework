# Agent Work Log — 2026-08-13

## Repository and Policy documentation context recovery

Status: `RESOLVED`

- Re-read the repository-root and package `AGENTS.md`, all existing daily work
  logs, and every Markdown document under `docs/`.
- Confirmed that the current handoff is centered on the 2026-08-12 OMC
  operator-store/serialization failures, the isolated legacy ONNX environment,
  optional Gemm decomposition and quant-name coverage, and removal of exported
  Pow nodes. Earlier ScatterND and MRoPE failures are retained as historical
  context rather than treated as the latest terminal blocker.
- No model code or existing documentation was changed during this context-only
  review. Existing modified and untracked files were preserved.

Validation evidence: both instruction files and all 11 pre-existing Markdown
files under `docs/` were read locally; no ONNX, CUDA, OMG, OMC, or server-side
validation was performed.

## ONNX external-data `.pb` naming

Status: `RESOLVED`

- ONNX external tensor data does not require a `.data` suffix. A single
  `model.onnx.pb` external file is valid when the main ONNX tensor metadata
  records that relative filename in each external-data `location` field.
- Renaming only the existing `.onnx.data` file is insufficient because the
  main model would retain stale `.data` references. The current exporter and
  simplification repack path explicitly choose `<model>.onnx.data`, while the
  shell pipeline defaults OMG `--weight` to that same path.
- Supporting `.onnx.pb` cleanly requires parameterizing the converter's
  external-data filename and passing the resolved path to OMG. The `.pb` file
  remains raw external tensor bytes; the suffix does not turn it into another
  ONNX ModelProto.

Validation evidence: inspected `convert_cosmos3_to_onnx.py` external-data
consolidation and `run_cosmos3_quant_onnx_full.sh` Step 4/9 path handling. No
model was repacked and no ONNX checker, ORT, OMG, or OMC validation was run.

## Selectable `.onnx.pb` external-data output

Status: `INVESTIGATING`

- The full quantization pipeline now defaults
  `COSMOS3_EXTERNAL_DATA_USE_PB=1`, producing
  `edge_policy.int8_fakequant.compatible.named.onnx.pb` beside the final ONNX;
  setting the switch to `0` restores the former raw `.onnx.data` filename.
- Step 4 passes the selected path into `convert_cosmos3_to_onnx.py`. Both the
  initial consolidation and the post-`onnxslim` repack write that filename into
  ONNX external-data `location` metadata. Step 9 uses the same path for OMG
  `--weight`, and Step-5 resume now fails early if the selected sidecar is
  absent.
- The converter accepts `--external-data-path`, requires the external file to
  be beside the main ONNX, and rejects using the model path itself. The `.pb`
  payload remains raw ONNX external tensor bytes.

Affected files: `onnxsrc/run_cosmos3_quant_onnx_full.sh`,
`onnxsrc/convert_cosmos3_to_onnx.py`, and `onnxsrc/README.md`.

Local validation: Python compilation, Bash syntax, and whitespace checks
passed. Mocked resume-through-OMG control-flow runs selected `.onnx.pb` by
default and `.onnx.data` with the switch disabled; an invalid switch exited 2
before path checks. The local environment has no `onnx` package, so a real
large-model repack, checker, ORT comparison, and OMG/OMC conversion remain
pending on the GPU server.

## Publish selectable `.onnx.pb` output

Status: `RESOLVED`

- The initial publication attempt appeared blocked because the restricted
  network sandbox could not reach the GitHub API. Browser reauthorization
  completed successfully, and an unrestricted API check confirmed the active
  `cchensjtu0821-netizen` account.
- Local `main` and `origin/main` both currently point to `14e16cd`; no branch
  divergence was present when publication was attempted.
- The scoped PB-output implementation, its README update, and this daily log
  are eligible for direct publication to `origin/main`; unrelated modified and
  untracked files remain excluded.

## ONNX opset 12 compatibility question

Status: `RESOLVED`

- External-data serialization and ONNX opset are independent. Choosing a
  `.pb` `location` in `onnx.save_model` does not convert or constrain the
  model's `opset_import`.
- The current Policy exporter intentionally targets opset 18. It cannot be
  safely changed to 12 as a filename/metadata-only adjustment because Step 5
  creates `ScatterND(reduction=add)` as an exact intermediate lowering, while
  the `reduction` attribute is only defined for ScatterND starting at opset 16.
  Step 5 runs `onnx.checker` before Step 6 lowers that intermediate, so an
  opset-12 declaration would be structurally invalid in the current sequence.
- If a deployment tool strictly requires opset 12, it needs a separate
  compatibility project: export/down-convert a candidate, rewrite every
  post-opset-12 schema use before declaring opset 12, then run checker,
  structural audit, original-versus-candidate output comparison, and OMG/OMC.
  Merely editing `opset_import` is not a valid conversion.
- Opset 12 was introduced with ONNX 1.7.0/IR 7, but the Python package does
  not have to be exactly 1.7. A newer ONNX package can load and check an
  opset-12 graph. Backend acceptance must be checked independently for both
  `ModelProto.ir_version` and every domain in `opset_import`; an older compiler
  may accept opset 12 yet reject a newer IR version emitted by a modern
  exporter. ONNX Runtime 1.16.3 is broadly new enough for opset 12, but that
  does not establish private OMG/OMC support.

No code was changed for opset selection. Local ONNX validation remains
unavailable because this workspace has no `onnx` package.

## Persist OMG terminal output

Status: `RESOLVED`

- Step 9 now sends combined OMG stdout and stderr through `tee`, preserving
  live terminal output while saving the complete stream to
  `${COSMOS3_QUANT_ROOT}/omg.log` by default.
- `COSMOS3_OMG_LOG` can select another path; its parent directory is created
  before OMG starts, and the completed pipeline reports the resolved log path.
- The script already enables `set -o pipefail`, so an OMG failure remains a
  pipeline failure rather than being hidden by a successful `tee` process.

Affected files: `onnxsrc/run_cosmos3_quant_onnx_full.sh` and
`onnxsrc/README.md`. Runtime OMG logs remain untracked deployment artifacts and
must not be committed.

Local validation: Bash syntax and whitespace checks passed. A mocked successful
OMG run showed the same complete command output in the terminal and saved log;
a mocked failing OMG command wrote stderr to the log, returned its nonzero
status through `tee`, and stopped before cleanup. Real OMG/OMC execution remains
server-side.
