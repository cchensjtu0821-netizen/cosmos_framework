# Agent Work Log — 2026-08-15

## Synthetic fixed-layout OMC input binaries

Status: `RESOLVED`

- The downstream OMC integration needs five headerless FP16 inputs matching
  `video_latent:[48,9,33,40]`, `action_latent:[33,64]`,
  `vision_timestep:[2720]`, `action_timestep:[32]`, and
  `prompt_embeddings:[108,2048]`.
- Added a deterministic, Python-standard-library-only generator that writes
  one little-endian, C-order `.bin` per input and a manifest containing order,
  sizes, hashes, and summary statistics. Random values are explicitly
  smoke-test data, not real Policy preprocessing output.
- The generator refuses to replace existing files unless `--overwrite` is
  supplied and supports configurable seed and repeated timestep value.
- Affected files: `onnxsrc/generate_omc_input_bins.py`, its focused test, and
  `onnxsrc/README.md`.
- Validation: Python 3.10 compilation and three focused unit tests passed. A
  real CLI run generated all five files; independent standard-library reads
  confirmed each byte count is `product(shape) * 2` and both timestep files
  contain the requested FP16 value. Ruff was unavailable in the local
  workspace; line-length and repository whitespace checks passed. OMC
  execution remains downstream/server-side.
