# Agent Work Log — 2026-08-31

## `RESOLVED` 320×192 Edge Policy ONNX layout

- Read the repository/package instructions, Policy ONNX shape documentation,
  prior ONNX work logs, exporter, preprocessing, packing, rewrite, and full
  quantization/conversion pipeline.
- `320×192` (width×height) is already the canonical 16:9 target in resolution
  tier `"256"`; no resolution-table, VAE-weight, or VFM-weight change is
  required. Leaving `resolution="480"` would pad/resize back to a 480-tier
  canvas and would not produce the requested small layout.
- With the existing 32-action request, the expected cropped latent is
  `[1,48,9,12,20]`, the patch grid is `[9,6,10]`, vision tokens are `540`, and
  `vision_timestep` is `[480]` when latent frame 0 is conditioned. The exact
  prompt length and final graph I/O must come from the new export manifest.
- The fixed packed layout embeds sequence indexes, MRoPE, masks, and timestep
  sizes, so changing only OMG `--input_shape` is invalid. Generate a new FP32
  layout manifest and rerun quantization/export from Steps 1–4; do not resume
  an old raw ONNX at Step 5.
- Required configuration surfaces are the exporter/server image height, image
  width, and resolution; the full pipeline's layout manifest/output root and
  OMG input shape; and any synthetic OMC input generator/host buffers. The
  rewrite and finalization passes are shape-driven and should be revalidated,
  not hard-coded for the old spatial size.
- The activation and packed-sequence dimensions are much smaller, but ONNX
  weight size remains almost unchanged because the Edge backbone parameters
  do not change. Server-side checker, structural audit, original/rewritten ORT
  comparison, and OMG/OMC conversion remain required.
- The exporter default checkpoint is Nano Policy, so the 320x192 Edge export
  must pass the Edge checkpoint explicitly. An existing `.onnx.json` is a
  generated manifest and must not be edited to change layout; rerunning the
  exporter creates a new manifest beside the new ONNX. The supplied historical
  manifest describes the old 480-tier layout and BF16 tensors despite its
  FP32-looking filename, so artifact dtype must be verified from the new
  manifest rather than inferred from the filename.
- The full quantization shell pipeline also has historical defaults for the
  old layout. Its invocation must override the new layout manifest, image
  height/width, resolution, 32-action request, FPS, and OMG input shape. Use a
  distinct 320x192 quant root because the pipeline cleans its selected output
  directory by default. On a host without OMG, run Steps 1--8 with
  `COSMOS3_RUN_OMG=0` and perform conversion separately on the OMG host.
- A direct OMG conversion must use the model, quant parameters, and external
  weights from the same new 320x192 run; changing only `--input_shape` cannot
  adapt an old fixed-layout graph. The new OMG shape changes vision latent
  spatial dimensions to `12,20` and vision timestep length to `480`. A
  `.transposed.pb` deployment sidecar exists only when the full pipeline ran
  with both Gemm decomposition and physical weight transposition enabled; a
  power-profile graph is an additional derived artifact and must likewise be
  regenerated from the new finalized graph.
- The full pipeline has no numbered step that creates a `.power_profile.onnx`,
  but the repository does contain the standalone
  `rewrite_omg_unsupported_patterns.py` postprocessor; its caller chooses the
  output suffix. The reported `.evaluation.json` schema and operator counts
  identify the power-profile artifact as the profiling-only rewrite with
  dynamic Sin/Cos bypassed, followed by the read-only analyzer. This graph is
  intentionally non-equivalent and only valid for structural power/performance
  evaluation. Its larger main protobuf comes from new inline materialized
  constants (constant Expands and constant trig outputs) plus one-hot spatial
  Conv weights, while learned model weights remain external. The transposed
  sidecar is separately created by the optional Step-5 branch. Historical
  directory names containing `16actions` are not reliable layout evidence when
  their graph/OMG shapes describe 33 action rows and 32 noised action timesteps.
- The current full pipeline automatically separates the released checkpoint's
  shared two-Linear timestep MLP after loading: vision and action receive deep
  copies with identical initial values but independent Parameters, ONNX
  initializers, consumers, and DOPT entries. Thus the source checkpoint has
  pairwise reuse, while the deployed ONNX intentionally does not. Steps 1, 3,
  and 4 all enter the same separation path; Step 2 rejects legacy shared names
  and Step 7 requires all four modality-specific Linear names. This is
  independent of the DOPT `embedding_separate` output-format switch.
- Git history clarifies why the unsupported-pattern postprocessor is not in the
  full shell pipeline. It was added later as an independent experimental stage:
  the exact mode still leaves four runtime-dependent Sin/Cos nodes and required
  server numerical/OMC validation, while the zero-trig profiling mode bypasses
  those values and is intentionally non-equivalent. Automatically applying it
  would silently turn a deployment graph into a power-only graph. The 2026-08-14
  log records server analyzer results and a manual exact post-rewrite audit, but
  not the exact command, artifact filename, or private path. The supplied
  zero-Sin/Cos `.evaluation.json` is new evidence that the profiling-only mode
  was also run later; the 2026-08-15 log records user-reported OMC success but
  does not identify which derived graph was converted.
- Server-reported 320x192 profiling rewrite completed successfully:
  `nodes=4293->4270`, `changes=124`, zero remaining dynamic trig nodes, and the
  report marks the graph profiling-only. The rewrite command's internal ONNX
  checker passed. Next actions are read-only analyzer/audit checks, then OMG on
  the separate conversion host using the matching new profile graph,
  transposed external-weight sidecar, quant parameters, and 320x192 input
  shape. This artifact remains non-equivalent and is not an accuracy model.
- The new profiling evaluation has 4,270 nodes versus 4,277 in the historical
  480-layout profile: `Concat 376->375`, `Slice 416->414`, and `Transpose
  234->230`; every other reported operator count is unchanged. This is expected
  because old latent height 33 required patch-size-2 padding to 34 and output
  cropping, whereas new spatial latent size 12x20 is already divisible by two.
  Identical Transformer operator counts confirm that the architecture is
  unchanged; only fixed-layout visual boundary plumbing and tensor extents are
  smaller.
- Resolution changes do not change the Edge backbone parameter/module schema,
  so quantized Linear key names and learned tensor shapes are expected to stay
  stable. However, node/shape plumbing, inline constants, calibration values,
  and possibly external-data packing order/offsets can differ. The PB sidecar
  contains tensor bytes rather than node names or quant keys; its binding is the
  initializer external-data metadata stored in the ONNX. Therefore each new
  ONNX, transposed PB, and quant-params file must remain a matched set even if
  two PB files happen to hash identically.
- Integrated the formerly manual power-profile flow into
  `run_cosmos3_quant_onnx_full.sh` behind explicit
  `COSMOS3_POWER_PROFILE=1`. Step 9a runs the unsupported-pattern rewrite with
  `--bypass-dynamic-trig-for-profiling`; Step 9b generates the evaluation report
  and strictly audits the derived graph. OMG now selects that profile graph only
  when the opt-in is enabled. The script header contains a complete 320x192
  example with Gemm decomposition, physical weight transposition, the new
  profiling switch, matching OMG shape, and separate-host OMG disabled.
  Completion output reports every profile artifact. Updated `onnxsrc/README.md`
  to document the non-equivalent boundary. Validation: Bash syntax and scoped
  whitespace checks pass, and 15 related pure structural tests pass under
  `python3`; `shellcheck` is unavailable. No local ONNX/CUDA/DOPT/OMG execution
  was performed.
- Extended `generate_omc_input_bins.py` with a saved `--layout 320x192` preset,
  replacing the fragile long inline-Python workaround while retaining the old
  480 layout as the default. The new preset emits video latent
  `[48,9,12,20]`, vision timestep `[480]`, and the unchanged action/prompt
  inputs, with a matching manifest. Updated its focused test and README command.
  Validation: Python compilation and all four generator tests pass; generated
  byte sizes equal `product(shape)*2` for every little-endian FP16 input.

Status: `RESOLVED` — the 320x192 BIN preset, opt-in two-stage power-profile
flow, and complete shell example are implemented and pass local static/unit
checks. Server-side ONNX/DOPT/OMG execution and power measurement remain the
deployment validation steps.
