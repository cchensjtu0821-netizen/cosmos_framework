# Agent Work Log — 2026-08-17

## Repository and Policy documentation context recovery

Status: `RESOLVED`

- Read the repository-root and package `AGENTS.md`, all seven Policy/ONNX
  documents under `docs/`, and all seven prior daily logs through 2026-08-15.
- Current handoff: the user reports that Policy conversion completed through
  OMC on a Python 3.10 server. Earlier ScatterND, MRoPE, and TBE/TVM failures
  remain historical diagnostic context; numerical equivalence and real-input
  deployment validation must still be distinguished from structural conversion.
- Preserved the existing mixed worktree and made no model or pipeline changes.
  No local ONNX, CUDA, ORT, OMG, OMC, checkpoint, or numerical validation was
  performed.

## Policy latency estimate from one ONNX denoise

Status: `RESOLVED`

- With the reported 3.3-second single-denoise latency, the default four-step
  sampler plus CFG performs eight network forwards and has a 26.4-second ONNX
  compute floor. End-to-end latency additionally includes VAE encode, packing,
  sampler updates, transfers, and action postprocessing; guidance 1 would use
  four forwards for a 13.2-second ONNX compute floor.

## Edge Policy component-size clarification

Status: `RESOLVED`

- Action postprocessing has no learned model or parameters; the action input/
  output heads are part of the denoiser.
- The Wan2.2 VAE architecture contains about 704.7M parameters in its full
  encoder/decoder module (about 149.6M on the encode path). Its released BF16
  weights occupy about 1.41 GB; the full FP32 checkpoint is about 2.82 GB.
- The released Edge transformer is 6.75 GB in BF16 including token embedding
  and language-model head. A fixed denoiser graph with prompt embeddings
  externalized and no language-model head is approximately 2.84B parameters:
  about 5.68 GB BF16, 11.35 GB FP32, or a 2.84 GB raw INT8 weight floor.
- Exact ONNX/OMC artifact size must be measured as the model file plus its
  external-data sidecar; graph metadata, quantization scales, alignment, and
  compiler packaging add overhead to the raw weight floor.

## `prepare_data_total` profiling interpretation

Status: `RESOLVED`

- `prepare_data_total` encloses sequence-plan construction, video/device
  normalization, VAE encode and output cast, conditional/unconditional text
  tokenization, initial packing, vision/action noise creation, conditioning
  blends, and flattened condition-buffer concatenation.
- The full 33-frame uint8 video is currently moved and converted to CUDA FP32
  before the causal-VAE prefix selection. For the recorded 544x736 layout this
  is about 39.6 MB of source data and 158.5 MB of FP32 output traffic outside
  the tight `vae_encode` region.
- Architecture-invariant noise is generated with NumPy on CPU and copied to
  the target device. Full-sized `initial_noise`, `condition_reference`, and
  `condition_mask` buffers also cause allocation and device-memory traffic.
- The parent is timed with CUDA events while it encloses CPU tokenizer and
  Python control flow. Its elapsed value is therefore an inclusive interval,
  not CUDA-kernel self time; parent-minus-VAE cannot be attributed solely to
  copies. A finer report should separately time video H2D/normalization,
  tokenizer/plan/pack CPU work, noise transfer/blending, and final concatenation.

## Cosmos3-Edge model-insight PPT first page

Status: `RESOLVED`

- The initial HTML-only draft was not an acceptable handoff: viewing the file
  through a source-oriented interface did not show the intended styling, and
  its card flow did not read as a real engineering block diagram.
- Superseded it with a native, editable 16:9 PowerPoint plus a pixel-matched PNG
  preview, using the html-ppt engineering-whiteprint visual language. The PPTX
  contains PowerPoint shapes, grouped stage containers, arrows, a CFG merge
  diamond, and a sampler feedback loop rather than flattened screenshot text.
- The inference diagram covers the local 32-action Edge path from
  `build_sample` through optional `vae_decode`, including observed video,
  action, latent, packed-sequence, noise, timestep, and output dimensions.
- Reworked the architecture after audience-readability feedback into one
  numbered main path: collapsed input/batching; `prepare_data_total` with
  separate video/text encoding and a merged pack/noise stage; `sampler_total`
  with explicit conditional/unconditional inputs, CFG merge, latent loop; and
  separate action/video output branches. The pack block now exposes the actual
  standard Policy token order `text -> vision -> action` and both packed lengths.
- The specification table compares Cosmos3-Edge, Cosmos3-Nano, and LingBot-VA
  using official model/paper parameter counts and architecture fields. Compute
  complexity is expressed as each system's actual denoising/CFG/AR execution
  structure rather than a misleading cross-model FLOPs scalar.
- The embodied-innovation section distinguishes Cosmos 3's shared
  reasoner/generator MoT, action-mode synergy, and domain-aware action
  projections from LingBot-VA's causal autoregressive rollout, KV cache, and
  asynchronous execution strengths.
- Validation: the generated PPTX reopens with `python-pptx` as one slide with
  290 editable shapes; the 1920x1080 PNG preview was rendered with a temporary
  CJK font and inspected for overlaps and connector routing. No GPU/ONNX
  validation applies to this presentation-only change.

Affected files: `docs/ppt/cosmos3_edge_model_insights/` and this log.
