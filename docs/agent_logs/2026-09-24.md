# Agent Log — 2026-09-24

## OPEN — MOZ1 may normalize already-normalized action values again

- User clarification: action values in the dataset were already transformed per channel using 1st/99th quantiles and clipped to [-1, 1] before this training job. The earlier diagnosis treated those stored values as unnormalized; it is superseded.
- Diagnosis: the recipe selects `action_normalization="quantile"`. The MOZ1 adapter concatenates the selected command fields verbatim (`leftarm_cmd_cart_pos`, gripper command, `rightarm_cmd_cart_pos`, gripper command, `torso_cmd_cart_pos`). `ActionSFTDataset` passes its normalizer into the transform, and `ActionProcessor.preprocess_action` applies it to all 17 rows. The adapter uses `norm_stats.state` for the first state row and `norm_stats.actions` for the 16 action rows. The second affine transform has no forward clamp. If those exact selected command fields contain the producer-normalized values, action targets are normalized twice.
- Evidence: `configs/base/experiment/action/posttrain_config/action_policy_moz1_edge.py`, `data/generator/action/datasets/moz1_lerobot_dataset.py`, `data/generator/action/datasets/action_sft_dataset.py`, `data/generator/action/transforms.py`, and `data/generator/action/action_processing.py`. Static code review only; the private dataset and running process were not inspected.
- Training impact: a second affine mapping would change the action targets and the `epsilon - x0` flow target; squared-error action loss can amplify the effect. The previous tail-clipping analysis is secondary and does not describe the primary risk if the stored action fields are already normalized.
- Added `scripts/audit_moz1_action_values.py` as a read-only Parquet sampler. It resolves the recipe's five command fields, prints observed per-dimension ranges/quantiles and exact ±1 boundary frequencies, and compares stored values with values after the current training affine transform. It does not decode video or write dataset files.
- Validation: Python AST parse and line-length check passed. The local workspace has no `MOZ1_ROOT`, `pyarrow`, or private dataset, so no actual Parquet sample was read here.
- Next action: run `python -m cosmos_framework.scripts.audit_moz1_action_values --root "$MOZ1_ROOT"` on the training server, inspect whether the stored pose channels are clipped at ±1 and whether the second transform materially changes them. Then decide how to handle the absolute state row and existing checkpoint/inference behavior before changing normalization.

## OPEN — MOZ1 offline open-loop Policy validation

- Goal: given simultaneous high/left-wrist/right-wrist keyframes, one 20-D current state, and task text, save predicted video and 16 future actions without robot execution or simulation.
- Diagnosis: generic Policy inference constructed only 16 zero action rows, while MOZ1 training used 17 rows with the first row containing the conditioned absolute state. Reusing the generic path unchanged would omit state conditioning and misalign the action timeline.
- Implementation: `inference/action.py`, `inference/args.py`, and `inference/inference.py` accept `state_path` and matching `norm_stats.json`, condition the first action row, add the MOZ1 camera prompt description, drop the state row from saved actions, and write `action.json`. Moved the shared state/action normalizer to `data/generator/action/action_processing.py`; the dataset imports it from there. Added `scripts/prepare_moz1_open_loop_input.py`, `inference/action_test.py`, and `docs/MOZ1_OPEN_LOOP_VALIDATION.md`.
- Validation: compileall and `git diff --check` passed. Local `pytest`/GPU inference are unavailable; server-side checkpoint inference and output inspection remain required. Output actions are in dataset-stored space, not necessarily physical robot command units, while the separate already-normalized-action audit remains OPEN.
